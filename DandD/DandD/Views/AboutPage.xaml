<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DandD.Views.AboutPage"
             xmlns:vm="clr-namespace:DandD.ViewModels;"
             Title="{Binding Title}">
  <ContentPage.BindingContext>
    <vm:AboutViewModel />
  </ContentPage.BindingContext>
  <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
      
        <ScrollView Grid.Row="1">
        <StackLayout Spacing="10" VerticalOptions="Center" HorizontalOptions="Center">
                <Image Source="http://i.imgur.com/EhMSDJm.jpg" />
                <Label Text="CPSC - 4910 - SQ"/>
                <Label Text="Students - Jia Hui Li, Chris Trent, Dhruv Seth" />
                <Label Text="Date/Time - 5/18, 10pm" />
            <Label Text="Code Review - Crystal Nguyen" />
        </StackLayout>
    </ScrollView>
    </Grid>
</ContentPage>